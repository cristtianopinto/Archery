@model IEnumerable<Archery.Models.Weapon>

@{
    ViewBag.Title = "Liste des armes";
}

<h2>Arc</h2>

<p>
    @Html.ActionLink("Ajouter une arme", "Create",null,new { @class="btn btn-info"})
</p>
<table class="table table-striped table-bordered ">
    <thead>
        <th colspan="2">
            @Html.DisplayNameFor(model => model.Name)
        </th>        
    <thead>
@foreach (var item in Model) {    
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })*@
                <a href="@Url.Action("edit", new { id=item.ID })"><i class="fas fa-pen"></i></a>
        
                <a href="@Url.Action("Details", new { id=item.ID })"><i class="fas fa-info"></i></a>
       
                <a href="@Url.Action("delete", new { id=item.ID })"><i class="fas fa-trash"></i></a>
            </td>
        </tr>}   
</table>
